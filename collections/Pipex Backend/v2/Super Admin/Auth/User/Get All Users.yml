info:
  name: Get All Users
  type: http
  seq: 5

http:
  method: GET
  url: "{{url}}/api/v2/superadmin/auth/user/all?limit=8"
  params:
    - name: limit
      value: "8"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
