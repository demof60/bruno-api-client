info:
  name: Verify Captcha
  type: http
  seq: 10

http:
  method: GET
  url: "{{url}}/api/v2/superadmin/auth/captcha"
  params:
    - name: searchTerm
      value: kuldeep
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
