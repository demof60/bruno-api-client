info:
  name: get admin profile
  type: http
  seq: 8

http:
  method: GET
  url: "{{url}}/api/v2/superadmin/auth/profile/admin/get"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
