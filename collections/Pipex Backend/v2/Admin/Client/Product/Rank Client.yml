info:
  name: Rank Client
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/v2/admin/client/product/ranking"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
