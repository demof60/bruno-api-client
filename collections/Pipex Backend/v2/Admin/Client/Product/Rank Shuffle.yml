info:
  name: Rank Shuffle
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/v2/admin/client/product/ranking/shuffle"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
