info:
  name: Reject subscription order
  type: http
  seq: 11

http:
  method: POST
  url: "{{url}}/api/v2/admin/payment/subscription/6a140043f4b6353f18b4d469/reject"
  params:
    - name: reqForCancel
      value: "true"
      type: query
      disabled: true
    - name: search
      value: Shaikh
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
