info:
  name: Export Subscription Orders CSV
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/v2/admin/payment/subscription/export/csv"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
