info:
  name: Cancel Subscription
  type: http
  seq: 7

http:
  method: POST
  url: "{{url}}/api/v2/admin/payment/subscription/{{subscriptionId}}/cancel"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
