info:
  name: Cancel Subscription
  type: http
  seq: 7

http:
  method: POST
  url: "{{url}}/api/v2/admin/payment/subscription/{{subscriptionId}}/cancel"
  auth: inherit

runtime:
  variables:
    - name: subscriptionId
      value: 6a118c38101784c459f5bd7f

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
