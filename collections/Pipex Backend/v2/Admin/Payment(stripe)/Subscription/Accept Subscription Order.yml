info:
  name: Accept Subscription Order
  type: http
  seq: 9

http:
  method: POST
  url: "{{url}}/api/v2/admin/subscription/{{subscriptionId}}/accept"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
