info:
  name: Get Specific plan
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/api/v2/admin/plan/package/get/specific/67a1a58654732350503de9d7"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
