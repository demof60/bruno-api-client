info:
  name: Get All Payment Plan
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/v2/admin/plan/payment/get"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
