info:
  name: Client List
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/v2/admin/statistics/campaigns/clients?limit=1&offset=0&search="
  params:
    - name: limit
      value: "1"
      type: query
    - name: offset
      value: "0"
      type: query
    - name: search
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
