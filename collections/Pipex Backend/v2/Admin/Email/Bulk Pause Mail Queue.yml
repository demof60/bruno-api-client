info:
  name: Bulk Pause Mail Queue
  type: http
  seq: 7

http:
  method: PUT
  url: "{{url}}/api/v2/admin/email/pause/bulk"
  body:
    type: json
    data: "{\r\n    \"ids\": \"all\"\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
