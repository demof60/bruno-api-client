info:
  name: fetch active product links
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/v2/admin/product/links/active"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
