info:
  name: Fetch update product links
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/v2/admin/product/links/update"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
