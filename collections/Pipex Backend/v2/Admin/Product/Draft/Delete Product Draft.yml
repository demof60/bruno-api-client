info:
  name: Delete Product Draft
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{url}}/api/v2/admin/product/draft/clear"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
