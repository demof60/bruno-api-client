info:
  name: Get Bin Products
  type: http
  seq: 8

http:
  method: GET
  url: "{{url}}/api/v2/admin/product/bin"
  body:
    type: form-urlencoded
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
