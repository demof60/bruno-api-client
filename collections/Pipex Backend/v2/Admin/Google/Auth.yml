info:
  name: Auth
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/v2/admin/google/auth"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
