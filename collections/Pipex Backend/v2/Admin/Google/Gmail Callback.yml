info:
  name: Gmail Callback
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/v2/admin/google/gmail/callback"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
