info:
  name: Reject Rfq
  type: http
  seq: 10

http:
  method: GET
  url: "{{url}}/api/v2/admin/report/rfq/reject/rfqId"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
