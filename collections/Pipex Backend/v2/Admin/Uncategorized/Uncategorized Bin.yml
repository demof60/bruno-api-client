info:
  name: Uncategorized Bin
  type: http
  seq: 7

http:
  method: GET
  url: "{{url}}/api/v2/admin/uncategorized/bin?limit=10&offset=0"
  params:
    - name: limit
      value: "10"
      type: query
    - name: offset
      value: "0"
      type: query
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
