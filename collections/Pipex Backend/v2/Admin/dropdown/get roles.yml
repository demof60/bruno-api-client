info:
  name: get roles
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/v2/admin/dropdown/role"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
