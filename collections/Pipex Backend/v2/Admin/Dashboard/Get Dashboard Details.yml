info:
  name: Get Dashboard Details
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/v2/admin/dashboard/get"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
