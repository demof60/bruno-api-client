info:
  name: Get live schedule categories
  type: http
  seq: 21

http:
  method: GET
  url: "{{url}}/api/v2/admin/folder/live/queue?limit=10&offset=980"
  params:
    - name: limit
      value: "10"
      type: query
    - name: offset
      value: "980"
      type: query
  body:
    type: multipart-form
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
