info:
  name: Fetch All Threads Count
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/v2/services/google-workspace/thread/count"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
