info:
  name: Generate Auth URL
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/v2/services/google-workspace/auth/generate-url"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
