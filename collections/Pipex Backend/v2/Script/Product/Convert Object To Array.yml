info:
  name: Convert Object To Array
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/v2/script/product/covert-object-to-array"
  auth:
    type: bearer
    token: "{{scriptToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
