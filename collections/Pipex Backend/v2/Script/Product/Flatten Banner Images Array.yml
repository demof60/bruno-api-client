info:
  name: Flatten Banner Images Array
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/v2/script/product/flatten-banner-images-array"
  auth:
    type: bearer
    token: "{{scriptToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
