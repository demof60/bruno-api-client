info:
  name: add location
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/api/v2/product/add-location"
  auth:
    type: bearer
    token: "{{scriptToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
