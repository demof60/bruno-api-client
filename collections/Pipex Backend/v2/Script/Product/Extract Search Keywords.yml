info:
  name: Extract Search Keywords
  type: http
  seq: 4

http:
  method: GET
  url: "{{scriptUrl}}/api/v2/product/extract-search-keywords"
  auth:
    type: bearer
    token: "{{scriptToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
