info:
  name: Logout User
  type: http
  seq: 7

http:
  method: POST
  url: "{{url}}/api/v2/buyer/auth/logout"
  body:
    type: json
    data: "{\r

      \    \"email\": \"vivek@pipex.com\",\r

      \    \"password\": \"Pipex@f60\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
