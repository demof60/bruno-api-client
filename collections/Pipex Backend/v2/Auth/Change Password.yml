info:
  name: Change Password
  type: http
  seq: 7

http:
  method: PUT
  url: "{{url}}/api/v2/auth/changePassword"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
