info:
  name: Verify OTP
  type: http
  seq: 6

http:
  method: POST
  url: "{{url}}/api/v2/auth/verifyotp"
  body:
    type: json
    data: "{\r\n    \"otp\":\"290678\"\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
