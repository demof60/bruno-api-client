info:
  name: Resend OTP
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/api/v2/auth/resendotp"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
