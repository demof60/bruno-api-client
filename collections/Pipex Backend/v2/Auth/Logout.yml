info:
  name: Logout
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/api/v2/auth/logout"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
