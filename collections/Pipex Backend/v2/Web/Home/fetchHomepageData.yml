info:
  name: fetchHomepageData
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/v2/web/home/get"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
