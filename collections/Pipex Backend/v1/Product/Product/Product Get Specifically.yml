info:
  name: Product Get Specifically
  type: http
  seq: 5

http:
  method: GET
  url: "{{url}}/v1/admin/productGet/669a484739fda7ad5ddccc86"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
