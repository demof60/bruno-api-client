info:
  name: Product Get Specifically
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/v1/user/productGet/stainless-steel-flanges-manufacturer-in-india"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
