info:
  name: Product Get
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/v1/user/productGet/fasterners-manufacturer-india"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
