info:
  name: Product Search based on search keywords (phase 2)
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:5000/api/v1/user/productSearch/flange
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
