info:
  name: Fetch Client Card
  type: http
  seq: 6

http:
  method: GET
  url: "{{url}}api/v1/user/product/clients/flanges-manufacturer-in-india"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
