info:
  name: Product and  category Count
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:5000/api/v1/productBinCount
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
