info:
  name: Category Search based on search keywords (phase 2)
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:5000/api/v1/admin/categorySearch/pipe
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
