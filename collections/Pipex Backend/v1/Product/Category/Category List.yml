info:
  name: Category List
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:5000/api/v1/categoryList
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
