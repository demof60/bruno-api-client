info:
  name: Category Get (personal)
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:5000/api/v1/admin/categoryGet/folder
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
