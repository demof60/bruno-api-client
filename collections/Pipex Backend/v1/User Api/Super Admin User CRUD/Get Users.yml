info:
  name: Get Users
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/v1/superAdmin/getUser?limit=10000&offset=0&type=inactive"
  params:
    - name: searchTerm
      value: shaqeeb
      type: query
      disabled: true
    - name: limit
      value: "10000"
      type: query
    - name: offset
      value: "0"
      type: query
    - name: type
      value: inactive
      type: query
      description: "[inactive,active]"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
