info:
  name: Get Single User by ID (refresh-purpose)
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:5000/api/v1/refreshPermissions/66791a72a910e43ddd5fd2f3
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
