info:
  name: Get Router Permissions
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:5000/api/v1/superAdmin/getRouterPermissions
  body:
    type: json
    data: "{\r

      \    \"routerEndpoint\": \"/supplierGet\"\r

      }"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
