info:
  name: change password
  type: http
  seq: 11

http:
  method: PUT
  url: "{{url}}/api/v1/user/changePassword"
  body:
    type: json
    data: "{\r

      \   \"oldPassword\": \"UqRk5$7%nNNI\",\r

      \   \"password\": \"Sanskar@2001\"\r

      }"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
