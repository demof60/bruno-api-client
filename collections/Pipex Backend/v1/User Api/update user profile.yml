info:
  name: update user profile
  type: http
  seq: 9

http:
  method: PUT
  url: "{{url}}/api/v1//user/profile"
  body:
    type: json
    data: "{\r\n  \r\n   \"role\":\"admin\"\r\n}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
