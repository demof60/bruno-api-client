info:
  name: Get User Profile
  type: http
  seq: 14

http:
  method: GET
  url: "{{url}}/api/v1/user/profile"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
