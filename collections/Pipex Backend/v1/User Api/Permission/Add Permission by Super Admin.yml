info:
  name: Add Permission by Super Admin
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:5000/api/v1/superAdmin/addPermission
  body:
    type: json
    data: "{\r

      \    \"permissionName\":\"read only\"\r

      \    // \"default\":\"not_default\" // optional\r

      }"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
