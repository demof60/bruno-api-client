info:
  name: Get all User (N/R)
  type: http
  seq: 15

http:
  method: GET
  url: "{{url}}/api/v1/userGetAll"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
