info:
  name: Get All Top Home Categories
  type: http
  seq: 11

http:
  method: GET
  url: "{{url}}/api/v1/web/home/folder?limit=10"
  params:
    - name: limit
      value: "10"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
