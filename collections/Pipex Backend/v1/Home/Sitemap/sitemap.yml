info:
  name: sitemap
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/v1/web/sitemap"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
