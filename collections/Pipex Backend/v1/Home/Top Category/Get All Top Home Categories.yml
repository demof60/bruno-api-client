info:
  name: Get All Top Home Categories
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/v1/web/home/folder"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
