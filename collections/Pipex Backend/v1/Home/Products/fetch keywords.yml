info:
  name: fetch keywords
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/v1/web/products/searchkeyword"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
