info:
  name: fetch top products
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/v1/web/products/viewedproducts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
