info:
  name: Fetch Filters
  type: http
  seq: 6

http:
  method: GET
  url: "{{url}}/api/v1/web/products/filter"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
