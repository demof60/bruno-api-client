info:
  name: Get Sectors
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/v1/sectors"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
