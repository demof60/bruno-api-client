info:
  name: Fetch All notification
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/v1/admin/notificationGetAll"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
