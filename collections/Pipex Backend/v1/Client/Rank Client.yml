info:
  name: Rank Client
  type: http
  seq: 9

http:
  method: GET
  url: "{{url}}/api/v1/client/ranking"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
