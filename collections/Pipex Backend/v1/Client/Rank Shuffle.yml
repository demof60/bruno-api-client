info:
  name: Rank Shuffle
  type: http
  seq: 8

http:
  method: GET
  url: "{{url}}/api/v1/client/ranking/shuffle"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
