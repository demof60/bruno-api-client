info:
  name: Get All Plans
  type: http
  seq: 1

http:
  method: GET
  url: https://backend.f10.co.in/api/v1/getAllPlan
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
