info:
  name: Supplier Keyword search
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:5000/api/v1/supplierLiveSearch/bucket
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
