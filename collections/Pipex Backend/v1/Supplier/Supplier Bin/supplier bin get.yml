info:
  name: supplier bin get
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:5000/api/v1/supplierBinGet
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
