info:
  name: SupplierGet
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5000/api/v1/supplierGet?limit=10&offset=0
  params:
    - name: limit
      value: "10"
      type: query
    - name: offset
      value: "0"
      type: query
  body:
    type: json
    data: "{\r

      \    \"type\": \"all\",\r

      \    \"searchQuery\": \"\"\r

      }"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
