info:
  name: Delete a folder
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{url}}/api/v1/folder/669a34cd9de356fe56f4b353"
  body:
    type: json
    data: "{\r

      \    \"type\": \"deleteTree\"\r

      }"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
