info:
  name: Flatten Banner Images Array
  type: http
  seq: 19

http:
  method: GET
  url: http://localhost:4060/api/script/flatten-banner-images-array
  auth:
    type: bearer
    token: "{{scriptToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
