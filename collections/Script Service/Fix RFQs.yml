info:
  name: Fix RFQs
  type: http
  seq: 12

http:
  method: GET
  url: "{{scriptUrl}}/api/v2/product/fix-rfq"
  auth:
    type: bearer
    token: "{{scriptToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
