info:
  name: Find Duplicate Data
  type: http
  seq: 23

http:
  method: GET
  url: "{{scriptUrl}}/api/v2/find-duplicate"
  auth:
    type: bearer
    token: "{{sellerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
