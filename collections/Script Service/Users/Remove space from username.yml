info:
  name: Remove space from username
  type: http
  seq: 1

http:
  method: GET
  url: "{{scriptUrl}}/api/v2/user/remove-space-username"
  auth:
    type: bearer
    token: "{{scriptToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
