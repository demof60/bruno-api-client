info:
  name: Sanitize Endpoints
  type: http
  seq: 3

http:
  method: GET
  url: "{{scriptUrl}}/api/v2/category/sanitize-endpoints"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
