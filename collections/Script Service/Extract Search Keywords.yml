info:
  name: Extract Search Keywords
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:4060/api/script/extract-search-keywords
  auth:
    type: bearer
    token: "{{scriptToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
