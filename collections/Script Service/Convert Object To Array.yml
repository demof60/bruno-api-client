info:
  name: Convert Object To Array
  type: http
  seq: 18

http:
  method: GET
  url: http://localhost:4060/api/script/covert-object-to-array
  auth:
    type: bearer
    token: "{{scriptToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
