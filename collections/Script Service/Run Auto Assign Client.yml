info:
  name: Run Auto Assign Client
  type: http
  seq: 13

http:
  method: GET
  url: "{{scriptUrl}}/api/v2/product/autoassign/client"
  auth:
    type: bearer
    token: "{{scriptToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
