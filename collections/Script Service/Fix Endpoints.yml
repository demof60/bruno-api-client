info:
  name: Fix Endpoints
  type: http
  seq: 11

http:
  method: GET
  url: http://localhost:4060/api/v2/script/fix-endpoint
  auth:
    type: bearer
    token: "{{scriptToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
