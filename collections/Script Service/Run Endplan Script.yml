info:
  name: Run Endplan Script
  type: http
  seq: 15

http:
  method: GET
  url: "{{scripturl}}/api/v2/product/endplan-script"
  auth:
    type: bearer
    token: "{{scriptToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
