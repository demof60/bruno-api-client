info:
  name: Fix Product Endpoints
  type: http
  seq: 17

http:
  method: GET
  url: http://localhost:4060/api/v2/script/fix-product-endpoint
  auth:
    type: bearer
    token: "{{scriptToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
