info:
  name: fetch all active product links
  type: http
  seq: 3

http:
  method: GET
  url: "{{scriptUrl}}/api/v2/product/fetch-all-product-active-links"
  body:
    type: multipart-form
  auth:
    type: bearer
    token: "{{scriptToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
