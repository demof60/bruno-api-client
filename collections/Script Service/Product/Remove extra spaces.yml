info:
  name: Remove extra spaces
  type: http
  seq: 5

http:
  method: GET
  url: "{{scriptUrl}}/api/v2/product/remove-extra-spaces-for-filters"
  auth:
    type: bearer
    token: "{{scriptToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
