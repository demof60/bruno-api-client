info:
  name: Run Start Plan Script
  type: http
  seq: 4

http:
  method: GET
  url: "{{scripturl}}/api/v2/product/startplan-script"
  auth:
    type: bearer
    token: "{{scriptToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
