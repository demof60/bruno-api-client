info:
  name: Assign Free Plan
  type: http
  seq: 9

http:
  method: GET
  url: "{{scriptUrl}}/api/v2/client/expire-client-subscription"
  body:
    type: json
    data: |-
      {
        "clientIdOrName":["vinay d company"]
      }
  auth:
    type: bearer
    token: "{{scriptToken}}"

runtime:
  variables:
    - name: scriptToken
      value: "@AFxIrep*CWE&sva!Hug)bKTf(cz6PJQmt7dDkyUB%"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
