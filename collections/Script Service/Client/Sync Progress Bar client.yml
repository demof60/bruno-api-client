info:
  name: Sync Progress Bar client
  type: http
  seq: 2

http:
  method: GET
  url: "{{scriptUrl}}/api/v2/client/sync-progressbarstrength"
  auth:
    type: bearer
    token: "{{scriptToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
