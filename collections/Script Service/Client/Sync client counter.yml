info:
  name: Sync client counter
  type: http
  seq: 1

http:
  method: GET
  url: "{{scriptUrl}}/api/v2/client/sync-client-counter"
  auth:
    type: bearer
    token: "{{scriptToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
