info:
  name: Add Default Steps
  type: http
  seq: 1

http:
  method: POST
  url: "{{scriptUrl}}/api/v2/billing/add-default-steps"
  body:
    type: json
    data: |-
      {
        "clientIds" : ["6a1448d7fcad7f5677da0724"]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
