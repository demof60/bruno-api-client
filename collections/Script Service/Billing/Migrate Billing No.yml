info:
  name: Migrate Billing No
  type: http
  seq: 2

http:
  method: GET
  url: "{{scriptUrl}}/api/v2/billing/transform-billing-number"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
