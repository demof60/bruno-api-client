info:
  name: Flatten Endpoints
  type: http
  seq: 16

http:
  method: GET
  url: http://localhost:4060/api/v2/product/flatten-endpoint
  auth:
    type: bearer
    token: "{{scriptToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
