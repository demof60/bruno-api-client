info:
  name: Migration
  type: http
  seq: 21

http:
  method: GET
  url: "{{scriptUrl}}/api/v2/migration/getMigrations"
  auth:
    type: bearer
    token: "{{scriptToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
