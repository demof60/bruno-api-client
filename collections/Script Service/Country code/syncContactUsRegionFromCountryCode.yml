info:
  name: syncContactUsRegionFromCountryCode
  type: http
  seq: 5

http:
  method: POST
  url: "{{scriptUrl}}/api/v2/countryCode/syncContactUsRegionFromCountryCode"
  auth:
    type: bearer
    token: "{{sellerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
