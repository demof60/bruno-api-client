info:
  name: syncLeadCountryCode
  type: http
  seq: 1

http:
  method: POST
  url: "{{scriptUrl}}/api/v2/countryCode/syncCountryCode"
  auth:
    type: bearer
    token: "{{sellerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
