info:
  name: syncAdditionalInfoRegion
  type: http
  seq: 7

http:
  method: POST
  url: "{{scriptUrl}}/api/v2/countryCode/syncAdditionalInfoRegion"
  auth:
    type: bearer
    token: "{{sellerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
