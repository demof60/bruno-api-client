info:
  name: syncVcardRegionFromCountryCode
  type: http
  seq: 3

http:
  method: POST
  url: "{{scriptUrl}}/api/v2/countryCode/syncVcardRegionFromCountryCode"
  auth:
    type: bearer
    token: "{{sellerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
