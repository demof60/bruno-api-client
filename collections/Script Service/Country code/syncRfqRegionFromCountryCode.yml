info:
  name: syncRfqRegionFromCountryCode
  type: http
  seq: 4

http:
  method: POST
  url: "{{scriptUrl}}}/api/v2/countryCode/syncRfqRegionFromCountryCode"
  auth:
    type: bearer
    token: "{{sellerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
