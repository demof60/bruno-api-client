info:
  name: generate text
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/v2/ai/generateText?text=Inconel Flat Bar"
  params:
    - name: text
      value: Inconel Flat Bar
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
