info:
  name: generate specific Field Text
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/v2/ai/generateSpecificFieldText?text=Inconel Flat Bar&fieldName=metaTitle"
  params:
    - name: text
      value: Inconel Flat Bar
      type: query
    - name: fieldName
      value: metaTitle
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
