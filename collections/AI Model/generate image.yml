info:
  name: generate image
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/v2/ai/generateImage?text=A character doctor strange fighting with villain like docker and jenkins "
  params:
    - name: testing
      value: ""
      type: query
      disabled: true
    - name: text
      value: "A character doctor strange fighting with villain like docker and jenkins "
      type: query
  body:
    type: multipart-form
    data:
      - name: testing
        type: file
        value:
          - postman-cloud:///1f00ef26-452a-4e20-ae6c-55c3b5be7937
        disabled: true
      - name: text
        type: text
        value: "A character doctor strange fighting with villain like docker and jenkins "
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
