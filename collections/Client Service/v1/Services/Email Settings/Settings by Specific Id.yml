info:
  name: Settings by Specific Id
  type: http
  seq: 3

http:
  method: GET
  url: "{{clientUrl}}/api/v1/services/google-workspace/setting/{Client id}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
