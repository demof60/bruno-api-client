info:
  name: Create Email Settings
  type: http
  seq: 1

http:
  method: POST
  url: "{{clientUrl}}/api/v1/services/google-workspace/email/setting"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
