info:
  name: Create Labels
  type: http
  seq: 2

http:
  method: POST
  url: "{{clientUrl}}/api/v1/unibox/label"
  body:
    type: json
    data: "{\r

      \    \"name\": \"personal label\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
