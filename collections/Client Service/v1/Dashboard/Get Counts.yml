info:
  name: Get Counts
  type: http
  seq: 1

http:
  method: GET
  url: "{{clientUrl}}/api/v1/dashboard/counts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
