info:
  name: Get Recent Lead
  type: http
  seq: 3

http:
  method: GET
  url: "{{clientUrl}}/api/v1/dashboard/recent-lead?limit=20"
  params:
    - name: limit
      value: "20"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
