info:
  name: Resend  OTP
  type: http
  seq: 7

http:
  method: POST
  url: "{{clientUrl}}/api/v1/auth/resendotp"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
