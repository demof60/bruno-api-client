info:
  name: Change Password
  type: http
  seq: 10

http:
  method: POST
  url: "{{clientUrl}}/api/v1/auth/changePassword"
  body:
    type: json
    data: "{\r

      \    \"oldPassword\":\"Pipex@F60\",\r

      \    \"password\":\"Pipex@f60\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
