info:
  name: Verify OTP
  type: http
  seq: 6

http:
  method: POST
  url: "{{clientUrl}}/api/v1/auth/verifyotp"
  body:
    type: json
    data: "{\r\n    \"otp\":\"426018\"\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
