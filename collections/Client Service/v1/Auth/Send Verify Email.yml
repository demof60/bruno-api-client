info:
  name: Send Verify Email
  type: http
  seq: 15

http:
  method: POST
  url: "{{clientUrl}}/api/v1/auth/send-verify-email"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
