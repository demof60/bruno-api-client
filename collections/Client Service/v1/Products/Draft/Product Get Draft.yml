info:
  name: Product Get Draft
  type: http
  seq: 1

http:
  method: GET
  url: "{{clientUrl}}/api/v1/product/draft/get"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
