info:
  name: Bin Product list
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:9000/api/v1/product/stash/get?limit=10&offset=0
  params:
    - name: limit
      value: "10"
      type: query
    - name: offset
      value: "0"
      type: query
    - name: search
      value: ""
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
