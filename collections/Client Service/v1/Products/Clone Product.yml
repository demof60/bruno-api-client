info:
  name: Clone Product
  type: http
  seq: 9

http:
  method: POST
  url: "{{clientUrl}}/api/v1/product/clone"
  body:
    type: json
    data: |-
      {

           "productId": "6a2c0685b1e066075d0ea9db"
          // "newProductTitle":"mast clone product h",
          // "newCategoryId":"672855068ff573adb3544f88"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
