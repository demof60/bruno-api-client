info:
  name: Add Product Live
  type: http
  seq: 6

http:
  method: POST
  url: "{{clientUrl}}/api/v1/product/live/:ProductId"
  params:
    - name: ProductId
      value: 6a1fd8c60f280d1d33de5da8
      type: path
  body:
    type: multipart-form
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
