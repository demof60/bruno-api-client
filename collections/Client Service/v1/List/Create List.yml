info:
  name: Create List
  type: http
  seq: 1

http:
  method: POST
  url: "{{clientUrl}}/api/v1/list"
  body:
    type: json
    data: "{\r

      \    \"name\":\"Pipex.ai shubham\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
