info:
  name: Get All  List
  type: http
  seq: 4

http:
  method: GET
  url: "{{clientUrl}}/api/v1/list?offset=0"
  params:
    - name: searchTerm
      value: borivali
      type: query
      disabled: true
    - name: offset
      value: "0"
      type: query
    - name: limit
      value: "10"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
