info:
  name: Get List
  type: http
  seq: 3

http:
  method: GET
  url: "{{clientUrl}}/api/v1/staff?limit=1000&status=all"
  params:
    - name: searchTerm
      value: salma
      type: query
      disabled: true
    - name: offset
      value: ""
      type: query
      disabled: true
    - name: limit
      value: "1000"
      type: query
    - name: status
      value: all
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
