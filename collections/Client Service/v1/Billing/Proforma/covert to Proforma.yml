info:
  name: covert to Proforma
  type: http
  seq: 12

http:
  method: POST
  url: "{{clientUrl}}/api/v1/proforma/:billingId?fromType=quotation"
  params:
    - name: fromType
      value: quotation
      type: query
    - name: billingId
      value: 698ec0796d32aee23b2a2a4e
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
