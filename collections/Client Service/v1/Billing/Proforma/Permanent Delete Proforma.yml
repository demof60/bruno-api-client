info:
  name: Permanent Delete Proforma
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{clientUrl}}/api/v1/proforma/permanent?type=proforma"
  params:
    - name: type
      value: proforma
      type: query
  body:
    type: json
    data: "{\r\n    \"ids\":[\"all\"]\r\n}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
