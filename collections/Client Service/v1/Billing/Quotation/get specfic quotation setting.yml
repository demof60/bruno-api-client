info:
  name: get specfic quotation setting
  type: http
  seq: 20

http:
  method: GET
  url: "{{clientUrl}}/api/v1/quotation/billing/settings/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
