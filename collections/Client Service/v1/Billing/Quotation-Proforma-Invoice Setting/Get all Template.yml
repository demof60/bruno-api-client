info:
  name: Get all Template
  type: http
  seq: 9

http:
  method: GET
  url: "{{clientUrl}}/api/v1/billing-template"
  params:
    - name: type
      value: quotation
      type: query
      disabled: true
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
