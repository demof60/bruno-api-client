info:
  name: POST metrics
  type: http
  seq: 2

http:
  method: POST
  url: "{{clientUrl}}/api/v1/metrics"
  body:
    type: json
    data: "{\r\n  \"name\": \"Box\"\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
