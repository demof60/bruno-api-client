info:
  name: Bulk Exports  Lead (excel)
  type: http
  seq: 5

http:
  method: POST
  url: "{{clientUrl}}/api/v1/lead/bulk/excel/exports"
  body:
    type: multipart-form
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
