info:
  name: Get Transaction History
  type: http
  seq: 5

http:
  method: GET
  url: "{{clientUrl}}/api/v1/payment/transaction/history"
  params:
    - name: export
      value: "true"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
