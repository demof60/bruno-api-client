info:
  name: Get Active Plan
  type: http
  seq: 7

http:
  method: GET
  url: "{{clientUrl}}/api/v1/payment/active/plan"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
