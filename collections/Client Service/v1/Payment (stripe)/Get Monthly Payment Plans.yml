info:
  name: Get Monthly Payment Plans
  type: http
  seq: 3

http:
  method: GET
  url: "{{clientUrl}}/api/v1/payment/plans/duration?sellerPlan=true"
  params:
    - name: sellerPlan
      value: "true"
      type: query
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
