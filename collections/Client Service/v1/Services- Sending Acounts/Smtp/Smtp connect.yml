info:
  name: Smtp connect
  type: http
  seq: 1

http:
  method: POST
  url: "{{clientUrl}}/api/v1/services/google-workspace/smtp/connect"
  body:
    type: json
    data: |-
      {
        "email":"shaqeeb@pentagoat.com",
        "appPassword":"nuzi eopc iofj xlzp",
        "host":"smtp.gmail.com",
        "label":"billing"

      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
