info:
  name: Delete Setting
  type: http
  seq: 4

http:
  method: GET
  url: "{{clientUrl}}/api/v1/services/google-workspace/setting/{Client id}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
