info:
  name: Get Catalog
  type: http
  seq: 4

http:
  method: GET
  url: "{{clientUrl}}/api/v1/catalog/get"
  body:
    type: multipart-form
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
