info:
  name: get activity
  type: http
  seq: 1

http:
  method: GET
  url: "{{clientUrl}}/api/v1/activity?limit=10&offset=0"
  params:
    - name: limit
      value: "10"
      type: query
    - name: offset
      value: "0"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
