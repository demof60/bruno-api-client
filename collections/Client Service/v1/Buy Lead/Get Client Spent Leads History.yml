info:
  name: Get Client Spent Leads History
  type: http
  seq: 6

http:
  method: GET
  url: "{{clientUrl}}/api/v1/buyleads/lead-spent-history"
  params:
    - name: searchTerm
      value: wwwwww
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
