info:
  name: get total & remaining leads
  type: http
  seq: 2

http:
  method: GET
  url: "{{clientUrl}}/api/v1/buyleads/total-and-remaining-leads"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
