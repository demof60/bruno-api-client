info:
  name: List  Dropdown
  type: http
  seq: 1

http:
  method: GET
  url: "{{clientUrl}}/api/v1/dropdown/list?limit=5&offset=0"
  params:
    - name: limit
      value: "5"
      type: query
    - name: offset
      value: "0"
      type: query
    - name: searchTerm
      value: borivali
      type: query
      disabled: true
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
