info:
  name: Get User Profile
  type: http
  seq: 10

http:
  method: GET
  url: "{{clientUrl}}/api/v1/profile/user/get"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
