info:
  name: Get V card
  type: http
  seq: 12

http:
  method: GET
  url: "{{clientUrl}}/api/v1/profile/vcard"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
