info:
  name: Client Endpoint Get
  type: http
  seq: 2

http:
  method: GET
  url: "{{clientUrl}}/api/v1/product/endpoint/get"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
