info:
  name: Get Client Company Profile
  type: http
  seq: 8

http:
  method: GET
  url: "{{clientUrl}}/api/v1/profile/getProfile"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
