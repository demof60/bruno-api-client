info:
  name: Client Free Plan activation
  type: http
  seq: 5

http:
  method: GET
  url: "{{clientUrl}}/api/v1/profile/live/free"
  body:
    type: multipart-form
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
