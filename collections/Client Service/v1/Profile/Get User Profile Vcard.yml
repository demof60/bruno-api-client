info:
  name: Get User Profile Vcard
  type: http
  seq: 11

http:
  method: GET
  url: "{{clientUrl}}/api/v1/profile/user/vcard"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
