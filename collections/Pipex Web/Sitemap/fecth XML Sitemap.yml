info:
  name: fecth XML Sitemap
  type: http
  seq: 2

http:
  method: GET
  url: "{{webUrl}}/api/v1/sitemap/xml/products-1.xml"
  params:
    - name: search
      value: ""
      type: query
      disabled: true
  body:
    type: multipart-form
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
