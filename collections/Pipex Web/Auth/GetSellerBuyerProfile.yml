info:
  name: GetSellerBuyerProfile
  type: http
  seq: 2

http:
  method: GET
  url: "{{webUrl}}/api/v1/auth/profile"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
