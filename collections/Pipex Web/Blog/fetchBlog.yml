info:
  name: fetchBlog
  type: http
  seq: 2

http:
  method: GET
  url: "{{webUrl}}/api/v1/blog/updated-square-bar-weight-chart-2026-for-industrial-applications.html"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
