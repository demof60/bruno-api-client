info:
  name: fetchBlog
  type: http
  seq: 2

http:
  method: GET
  url: "{{webUrl}}/api/v1/blog/test-blog-2.html"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
