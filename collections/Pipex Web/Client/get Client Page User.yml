info:
  name: get Client Page User
  type: http
  seq: 1

http:
  method: GET
  url: "{{webUrl}}/api/v1/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
