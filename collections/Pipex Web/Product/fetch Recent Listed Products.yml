info:
  name: fetch Recent Listed Products
  type: http
  seq: 8

http:
  method: GET
  url: "{{webUrl}}/api/v1/products/recent-listed"
  params:
    - name: search
      value: ""
      type: query
      disabled: true
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
