info:
  name: get Top Products
  type: http
  seq: 4

http:
  method: GET
  url: "{{webUrl}}/api/v1/products/viewedproducts"
  params:
    - name: search
      value: ""
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
