info:
  name: fetch Main Categories
  type: http
  seq: 1

http:
  method: GET
  url: "{{webUrl}}/api/v1/category/main"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
