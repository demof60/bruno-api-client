info:
  name: fetch Main Categories Copy
  type: http
  seq: 2

http:
  method: GET
  url: "{{webUrl}}/api/v1/category/main"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
