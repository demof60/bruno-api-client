info:
  name: searchCategoriesAndProducts
  type: http
  seq: 3

http:
  method: GET
  url: "{{webUrl}}/api/v1/category/search?name=Wire Mesh"
  params:
    - name: name
      value: Wire Mesh
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
