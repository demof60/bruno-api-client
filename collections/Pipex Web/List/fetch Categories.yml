info:
  name: fetch Categories
  type: http
  seq: 1

http:
  method: GET
  url: "{{webUrl}}/api/v1/list/folders"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
