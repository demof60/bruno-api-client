info:
  name: fetch Locations
  type: http
  seq: 3

http:
  method: GET
  url: "{{webUrl}}/api/v1/list/locations"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
