info:
  name: fetch Industries
  type: http
  seq: 2

http:
  method: GET
  url: "{{webUrl}}/api/v1/list/industries"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
