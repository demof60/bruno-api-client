info:
  name: fetch All Trendy Products
  type: http
  seq: 1

http:
  method: GET
  url: "{{webUrl}}/api/v1/sector/get"
  params:
    - name: offset
      value: "0"
      type: query
      disabled: true
    - name: search
      value: ""
      type: query
      disabled: true
  body:
    type: multipart-form
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
