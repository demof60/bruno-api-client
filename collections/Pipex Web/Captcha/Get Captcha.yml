info:
  name: Get Captcha
  type: http
  seq: 1

http:
  method: GET
  url: "{{webUrl}}/api/v1/captcha"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
