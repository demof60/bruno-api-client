info:
  name: get Q&A
  type: http
  seq: 1

http:
  method: GET
  url: "{{userUrl}}/api/v1/dashboard/get/qa"
  params:
    - name: limit
      value: "10"
      type: query
      disabled: true
    - name: offset
      value: "0"
      type: query
      disabled: true
  body:
    type: text
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
