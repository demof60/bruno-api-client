info:
  name: Resend Otp
  type: http
  seq: 6

http:
  method: POST
  url: "{{userUrl}}/api/v1/auth/resendOtp"
  body:
    type: json
    data: "{\r\n    \r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
