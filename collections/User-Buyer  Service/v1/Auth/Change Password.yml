info:
  name: Change Password
  type: http
  seq: 10

http:
  method: PUT
  url: "{{userUrl}}/api/v1/auth/changePassword"
  body:
    type: json
    data: "{\r

      \    \"oldPassword\":\"Pipex@f60\",\r

      \    \"password\":\"Pipex@F60\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
