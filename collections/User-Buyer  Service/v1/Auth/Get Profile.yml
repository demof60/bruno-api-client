info:
  name: Get Profile
  type: http
  seq: 15

http:
  method: GET
  url: "{{userUrl}}/api/v1/auth/profile"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
