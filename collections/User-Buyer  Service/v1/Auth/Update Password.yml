info:
  name: Update Password
  type: http
  seq: 16

http:
  method: PUT
  url: "{{userUrl}}/api/v1/auth/changePassword"
  body:
    type: json
    data: "{\r

      \    \"oldPassword\":\"Pass@12345\", \"password\":\"Pass@12345\" \r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
