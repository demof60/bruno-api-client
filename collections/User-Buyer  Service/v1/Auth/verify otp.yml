info:
  name: verify otp
  type: http
  seq: 5

http:
  method: POST
  url: "{{userUrl}}/api/v1/auth/verifyOtp"
  body:
    type: json
    data: "{\r\n    \"otp\":\"394171\"\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
