info:
  name: Register
  type: http
  seq: 3

http:
  method: POST
  url: "{{userUrl}}/api/v1/auth/register?captcha=9"
  params:
    - name: captcha
      value: "9"
      type: query
  body:
    type: json
    data: "{\r

      \    \"password\": \"Pipex@F60\",\r

      \    \"email\": \"diqssla@denipl.com\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
