info:
  name: buyer top recommendation
  type: http
  seq: 2

http:
  method: GET
  url: "{{userUrl}}/api/v1/dashboard/get/top/recommendation?limit=10&offset=0"
  params:
    - name: limit
      value: "10"
      type: query
    - name: offset
      value: "0"
      type: query
  body:
    type: text
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
